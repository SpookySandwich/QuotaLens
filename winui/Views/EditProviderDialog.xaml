<ContentDialog
    x:Class="QuotaLens.Views.EditProviderDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QuotaLens.Views"
    mc:Ignorable="d"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    CornerRadius="{StaticResource OverlayCornerRadius}"
    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
    BorderThickness="1"
    DefaultButton="Primary"
    PrimaryButtonStyle="{StaticResource AccentButtonStyle}"
    CloseButtonStyle="{StaticResource CardActionButton}">
    <ContentDialog.Background>
        <AcrylicBrush TintColor="#2B2725"
                      TintOpacity="0.86"
                      TintLuminosityOpacity="0.72"
                      FallbackColor="#242120" />
    </ContentDialog.Background>
    <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="480">
        <StackPanel x:Name="FieldsPanel" Spacing="14" Width="420" MaxWidth="420" />
    </ScrollViewer>
</ContentDialog>
